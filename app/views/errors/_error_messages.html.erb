<% if target.errors.any? %>
<div id="error_explanation">
	<h2> Oops! There are at least <%= pluralize(target.errors.count, "error") %>.</h2>

	<ul>
		<% target.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>